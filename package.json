{
  "name": "jcode-ext-vm-manager",
  "version": "0.3.5",
  "private": true,
  "description": "VM Manager extension for JCode — a native Compose plugin drawn inside JCode's own process.",
  "scripts": {
    "build": "node build.mjs"
  }
}
