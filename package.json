{
  "name": "jcode-ext-vm-manager",
  "version": "0.3.3",
  "private": true,
  "description": "VM Manager extension for JCode — built with esbuild + TypeScript.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "typescript": "^5.5.4"
  }
}
